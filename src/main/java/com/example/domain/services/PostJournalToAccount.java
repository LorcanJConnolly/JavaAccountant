package com.example.domain.services;

public class PostJournalToAccount {

    // accountId, Journal
    // account = AccountManager.findAccountById(accountId)
    // value_before = account.getValue(now)
    // account.applyJournal.getTotal()
    // value_after = Journal.getTotal() + value_before
    // account.getHistory.addEvent(date : {value_after, Journal})
}
